PORT=3001
SESSION_SECRET=
CORS_ORIGIN=http://localhost:5173

DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=users

# Required for Aiven / most managed MySQL (set true on Railway)
DB_SSL=false
# Optional: set false only if you must skip CA verification
# DB_SSL_REJECT_UNAUTHORIZED=true
# Optional: paste Aiven CA PEM (use \n for newlines) if rejectUnauthorized needs it
# DB_SSL_CA=
